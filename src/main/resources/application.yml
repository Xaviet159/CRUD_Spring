spring:
  datasource:
    url: jdbc:postgresql://localhost:4242/spring_book_api
    username: postgres
    password: 159159
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    properties:
      hibernate:
        format_sql: true
server:
  port: 7070